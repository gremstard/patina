{
  "name": "patina",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "An open-world PSX-style driving/crime game. Bounded procedural world, built from primitives.",
  "scripts": {
    "test": "node --test",
    "worldindex": "node scripts/build-world-index.js",
    "bundle": "node scripts/bundle-web.js",
    "bundle:city": "node scripts/bundle-app.js city",
    "bundle:play": "node scripts/bundle-app.js play",
    "build": "node scripts/bundle-web.js && node scripts/bundle-app.js city && node scripts/bundle-app.js play",
    "smoke": "node scripts/bundle-web.js && node scripts/smoke-web.mjs",
    "smoke:city": "node scripts/bundle-app.js city && node scripts/smoke-app.mjs city",
    "smoke:play": "node scripts/bundle-app.js play && node scripts/smoke-app.mjs play ew"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "playwright-core": "^1.61.1"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
